# install
sudo apt update
sudo apt install mysql-server mysql-client
sudo service mysql start
sudo mysql



# setup user
CREATE DATABASE IF NOT EXISTS counter_app;
CREATE USER IF NOT EXISTS 'counter_app'@'localhost' IDENTIFIED BY '...';
GRANT SELECT, INSERT, UPDATE, DELETE, CREATE, DROP ON counter_app.* TO 'counter_app'@'localhost';
FLUSH PRIVILEGES;
